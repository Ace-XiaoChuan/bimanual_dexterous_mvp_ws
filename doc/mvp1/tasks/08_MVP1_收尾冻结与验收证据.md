# MVP-1 第八阶段：收尾冻结与验收证据

## 1. 工作目标

MVP-1 完成后，需要像 MVP-0 一样留下可复现的验收证据，而不是只说“代码写完”。

本阶段负责：

```text
干净环境回归
接口快照
测试结果记录
已知限制记录
完成清单
版本标签
```

## 2. 推荐验收目录

```text
doc/mvp1/acceptance/
├── mvp1_completion_checklist.md
├── build_and_test_result.md
├── interface_snapshot.md
└── known_limitations.md
```

## 3. 干净环境回归

冻结前必须执行：

```bash
cd /home/ace/bimanual_dexterous_mvp_ws

rm -rf build install log

source /opt/ros/humble/setup.bash

colcon build --symlink-install
source install/setup.bash

colcon test --event-handlers console_direct+
colcon test-result --verbose
```

## 4. 完成清单建议

`mvp1_completion_checklist.md` 至少包含：

```text
接口生成通过
fake_scene_manager 物体状态扩展完成
fake_hand_control 完成
fake_terminal_operation 完成
fake_arm_control 多目标完成
assembly_task Pick-and-Place 状态机完成
MVP-1 launch 完成
MVP-1 集成测试完成
MVP-0 回归通过
连续 10 次执行通过
干净环境回归通过
```

## 5. 构建和测试结果

`build_and_test_result.md` 至少记录：

```text
Git commit hash
ROS 2 Humble 版本
Python 版本
构建命令
构建结果
测试命令
测试结果
colcon test-result --verbose 摘要
失败项和处理方式
```

## 6. 接口快照

`interface_snapshot.md` 至少记录：

```text
StartTask 支持的 task_name
MoveArm 支持的 target_name
ControlHand 接口字段和 command
Scene object 接口字段和状态语义
TerminalOperation 支持的 operation_type
TaskState 状态序列
错误码表
launch 文件和节点列表
```

## 7. 已知限制

`known_limitations.md` 至少说明：

```text
仍然是纯软件 fake 系统
不控制真实 FR3
不控制真实 RH56 全关节
不做真实抓取、接触和物理仿真
不做 MoveIt / MTC 规划
不做 MuJoCo
不做航空插头插接
不做双臂协同
不支持任务取消、暂停、恢复和并发
```

## 8. README 更新

冻结后更新 README：

```text
当前阶段
MVP 阶段进度表
运行 MVP-1 的 launch 命令
MVP-1 验收记录链接
下一阶段 MVP-2 入口
```

README 不应复制完整验收日志；完整记录放入 `doc/mvp1/acceptance/`。

## 9. 版本标签

推荐冻结提交：

```bash
git add README.md doc/mvp1 src
git commit -m "chore: freeze MVP-1 fake pick-and-place baseline"
```

推荐标签：

```bash
git tag -a mvp1.0.0 -m "MVP-1 fake pick-and-place integration complete"
git push origin main
git push origin mvp1.0.0
```

## 10. 完成标准

MVP-1 可以宣告完成的条件：

```text
1. 干净环境 build 通过；
2. 全量 colcon test 通过；
3. MVP-1 集成测试覆盖成功链路；
4. MVP-0 回归测试仍通过；
5. 验收文档完整；
6. README 状态更新；
7. Git tag 可以定位冻结版本。
```
